<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fiesta.hevs.ch.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/fiesta"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity
            android:name=".FestivalActivity"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TransportActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".InfoTransportActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".ConversationListActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".TransportAltActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".NewTransportActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".UpdateTransportActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".CommunicationActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity android:name=".CommunicationListActivity"
            android:theme="@style/Theme.AppCompat"></activity>
    </application>

</manifest>